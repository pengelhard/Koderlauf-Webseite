# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Koderlauf-Webseite") is currently empty — it contains only a `README.md` with a project title. No application code, framework, package manager, or build tooling has been added yet.

### Current state (as of initial setup)

- **No source code** exists beyond `README.md`.
- **No dependencies** to install (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.).
- **No services** to run, no tests to execute, no linting configured.
- The project name suggests a German-language website project ("Koderlauf-Webseite").

### For future agents

Once application code is added to this repository, update this file with:
- The chosen framework and how to install dependencies.
- How to run the dev server, tests, and linting.
- Any non-obvious setup caveats discovered during development.
